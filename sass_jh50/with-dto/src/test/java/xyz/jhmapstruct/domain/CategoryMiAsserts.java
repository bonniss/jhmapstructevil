package xyz.jhmapstruct.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class CategoryMiAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCategoryMiAllPropertiesEquals(CategoryMi expected, CategoryMi actual) {
        assertCategoryMiAutoGeneratedPropertiesEquals(expected, actual);
        assertCategoryMiAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCategoryMiAllUpdatablePropertiesEquals(CategoryMi expected, CategoryMi actual) {
        assertCategoryMiUpdatableFieldsEquals(expected, actual);
        assertCategoryMiUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCategoryMiAutoGeneratedPropertiesEquals(CategoryMi expected, CategoryMi actual) {
        assertThat(expected)
            .as("Verify CategoryMi auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCategoryMiUpdatableFieldsEquals(CategoryMi expected, CategoryMi actual) {
        assertThat(expected)
            .as("Verify CategoryMi relevant properties")
            .satisfies(e -> assertThat(e.getName()).as("check name").isEqualTo(actual.getName()))
            .satisfies(e -> assertThat(e.getDescription()).as("check description").isEqualTo(actual.getDescription()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCategoryMiUpdatableRelationshipsEquals(CategoryMi expected, CategoryMi actual) {
        assertThat(expected)
            .as("Verify CategoryMi relationships")
            .satisfies(e -> assertThat(e.getTenant()).as("check tenant").isEqualTo(actual.getTenant()));
    }
}
