package ai.realworld.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class HexCharAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertHexCharAllPropertiesEquals(HexChar expected, HexChar actual) {
        assertHexCharAutoGeneratedPropertiesEquals(expected, actual);
        assertHexCharAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertHexCharAllUpdatablePropertiesEquals(HexChar expected, HexChar actual) {
        assertHexCharUpdatableFieldsEquals(expected, actual);
        assertHexCharUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertHexCharAutoGeneratedPropertiesEquals(HexChar expected, HexChar actual) {
        assertThat(expected)
            .as("Verify HexChar auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertHexCharUpdatableFieldsEquals(HexChar expected, HexChar actual) {
        assertThat(expected)
            .as("Verify HexChar relevant properties")
            .satisfies(e -> assertThat(e.getDob()).as("check dob").isEqualTo(actual.getDob()))
            .satisfies(e -> assertThat(e.getGender()).as("check gender").isEqualTo(actual.getGender()))
            .satisfies(e -> assertThat(e.getPhone()).as("check phone").isEqualTo(actual.getPhone()))
            .satisfies(e -> assertThat(e.getBioHeitiga()).as("check bioHeitiga").isEqualTo(actual.getBioHeitiga()))
            .satisfies(e -> assertThat(e.getIsEnabled()).as("check isEnabled").isEqualTo(actual.getIsEnabled()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertHexCharUpdatableRelationshipsEquals(HexChar expected, HexChar actual) {
        assertThat(expected)
            .as("Verify HexChar relationships")
            .satisfies(e -> assertThat(e.getRole()).as("check role").isEqualTo(actual.getRole()));
    }
}
