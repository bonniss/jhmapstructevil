package xyz.jhmapstruct.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class ShipmentViViAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertShipmentViViAllPropertiesEquals(ShipmentViVi expected, ShipmentViVi actual) {
        assertShipmentViViAutoGeneratedPropertiesEquals(expected, actual);
        assertShipmentViViAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertShipmentViViAllUpdatablePropertiesEquals(ShipmentViVi expected, ShipmentViVi actual) {
        assertShipmentViViUpdatableFieldsEquals(expected, actual);
        assertShipmentViViUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertShipmentViViAutoGeneratedPropertiesEquals(ShipmentViVi expected, ShipmentViVi actual) {
        assertThat(expected)
            .as("Verify ShipmentViVi auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertShipmentViViUpdatableFieldsEquals(ShipmentViVi expected, ShipmentViVi actual) {
        assertThat(expected)
            .as("Verify ShipmentViVi relevant properties")
            .satisfies(e -> assertThat(e.getTrackingNumber()).as("check trackingNumber").isEqualTo(actual.getTrackingNumber()))
            .satisfies(e -> assertThat(e.getShippedDate()).as("check shippedDate").isEqualTo(actual.getShippedDate()))
            .satisfies(e -> assertThat(e.getDeliveryDate()).as("check deliveryDate").isEqualTo(actual.getDeliveryDate()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertShipmentViViUpdatableRelationshipsEquals(ShipmentViVi expected, ShipmentViVi actual) {
        // empty method
    }
}
