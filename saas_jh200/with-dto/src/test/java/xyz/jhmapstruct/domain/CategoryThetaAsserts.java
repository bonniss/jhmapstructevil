package xyz.jhmapstruct.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class CategoryThetaAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCategoryThetaAllPropertiesEquals(CategoryTheta expected, CategoryTheta actual) {
        assertCategoryThetaAutoGeneratedPropertiesEquals(expected, actual);
        assertCategoryThetaAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCategoryThetaAllUpdatablePropertiesEquals(CategoryTheta expected, CategoryTheta actual) {
        assertCategoryThetaUpdatableFieldsEquals(expected, actual);
        assertCategoryThetaUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCategoryThetaAutoGeneratedPropertiesEquals(CategoryTheta expected, CategoryTheta actual) {
        assertThat(expected)
            .as("Verify CategoryTheta auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCategoryThetaUpdatableFieldsEquals(CategoryTheta expected, CategoryTheta actual) {
        assertThat(expected)
            .as("Verify CategoryTheta relevant properties")
            .satisfies(e -> assertThat(e.getName()).as("check name").isEqualTo(actual.getName()))
            .satisfies(e -> assertThat(e.getDescription()).as("check description").isEqualTo(actual.getDescription()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCategoryThetaUpdatableRelationshipsEquals(CategoryTheta expected, CategoryTheta actual) {
        assertThat(expected)
            .as("Verify CategoryTheta relationships")
            .satisfies(e -> assertThat(e.getTenant()).as("check tenant").isEqualTo(actual.getTenant()));
    }
}
