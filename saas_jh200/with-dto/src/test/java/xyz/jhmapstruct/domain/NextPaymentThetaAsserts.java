package xyz.jhmapstruct.domain;

import static org.assertj.core.api.Assertions.assertThat;
import static xyz.jhmapstruct.domain.AssertUtils.bigDecimalCompareTo;

public class NextPaymentThetaAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertNextPaymentThetaAllPropertiesEquals(NextPaymentTheta expected, NextPaymentTheta actual) {
        assertNextPaymentThetaAutoGeneratedPropertiesEquals(expected, actual);
        assertNextPaymentThetaAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertNextPaymentThetaAllUpdatablePropertiesEquals(NextPaymentTheta expected, NextPaymentTheta actual) {
        assertNextPaymentThetaUpdatableFieldsEquals(expected, actual);
        assertNextPaymentThetaUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertNextPaymentThetaAutoGeneratedPropertiesEquals(NextPaymentTheta expected, NextPaymentTheta actual) {
        assertThat(expected)
            .as("Verify NextPaymentTheta auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertNextPaymentThetaUpdatableFieldsEquals(NextPaymentTheta expected, NextPaymentTheta actual) {
        assertThat(expected)
            .as("Verify NextPaymentTheta relevant properties")
            .satisfies(e -> assertThat(e.getAmount()).as("check amount").usingComparator(bigDecimalCompareTo).isEqualTo(actual.getAmount()))
            .satisfies(e -> assertThat(e.getPaymentDate()).as("check paymentDate").isEqualTo(actual.getPaymentDate()))
            .satisfies(e -> assertThat(e.getPaymentMethod()).as("check paymentMethod").isEqualTo(actual.getPaymentMethod()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertNextPaymentThetaUpdatableRelationshipsEquals(NextPaymentTheta expected, NextPaymentTheta actual) {
        assertThat(expected)
            .as("Verify NextPaymentTheta relationships")
            .satisfies(e -> assertThat(e.getTenant()).as("check tenant").isEqualTo(actual.getTenant()));
    }
}
