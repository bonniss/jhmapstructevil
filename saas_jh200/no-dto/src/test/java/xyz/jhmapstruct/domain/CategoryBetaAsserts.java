package xyz.jhmapstruct.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class CategoryBetaAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCategoryBetaAllPropertiesEquals(CategoryBeta expected, CategoryBeta actual) {
        assertCategoryBetaAutoGeneratedPropertiesEquals(expected, actual);
        assertCategoryBetaAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCategoryBetaAllUpdatablePropertiesEquals(CategoryBeta expected, CategoryBeta actual) {
        assertCategoryBetaUpdatableFieldsEquals(expected, actual);
        assertCategoryBetaUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCategoryBetaAutoGeneratedPropertiesEquals(CategoryBeta expected, CategoryBeta actual) {
        assertThat(expected)
            .as("Verify CategoryBeta auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCategoryBetaUpdatableFieldsEquals(CategoryBeta expected, CategoryBeta actual) {
        assertThat(expected)
            .as("Verify CategoryBeta relevant properties")
            .satisfies(e -> assertThat(e.getName()).as("check name").isEqualTo(actual.getName()))
            .satisfies(e -> assertThat(e.getDescription()).as("check description").isEqualTo(actual.getDescription()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertCategoryBetaUpdatableRelationshipsEquals(CategoryBeta expected, CategoryBeta actual) {
        assertThat(expected)
            .as("Verify CategoryBeta relationships")
            .satisfies(e -> assertThat(e.getTenant()).as("check tenant").isEqualTo(actual.getTenant()));
    }
}
