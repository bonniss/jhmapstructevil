package xyz.jhmapstruct.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class NextShipmentThetaAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertNextShipmentThetaAllPropertiesEquals(NextShipmentTheta expected, NextShipmentTheta actual) {
        assertNextShipmentThetaAutoGeneratedPropertiesEquals(expected, actual);
        assertNextShipmentThetaAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertNextShipmentThetaAllUpdatablePropertiesEquals(NextShipmentTheta expected, NextShipmentTheta actual) {
        assertNextShipmentThetaUpdatableFieldsEquals(expected, actual);
        assertNextShipmentThetaUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertNextShipmentThetaAutoGeneratedPropertiesEquals(NextShipmentTheta expected, NextShipmentTheta actual) {
        assertThat(expected)
            .as("Verify NextShipmentTheta auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertNextShipmentThetaUpdatableFieldsEquals(NextShipmentTheta expected, NextShipmentTheta actual) {
        assertThat(expected)
            .as("Verify NextShipmentTheta relevant properties")
            .satisfies(e -> assertThat(e.getTrackingNumber()).as("check trackingNumber").isEqualTo(actual.getTrackingNumber()))
            .satisfies(e -> assertThat(e.getShippedDate()).as("check shippedDate").isEqualTo(actual.getShippedDate()))
            .satisfies(e -> assertThat(e.getDeliveryDate()).as("check deliveryDate").isEqualTo(actual.getDeliveryDate()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertNextShipmentThetaUpdatableRelationshipsEquals(NextShipmentTheta expected, NextShipmentTheta actual) {
        assertThat(expected)
            .as("Verify NextShipmentTheta relationships")
            .satisfies(e -> assertThat(e.getTenant()).as("check tenant").isEqualTo(actual.getTenant()));
    }
}
