package xyz.jhmapstruct.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class MasterTenantAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertMasterTenantAllPropertiesEquals(MasterTenant expected, MasterTenant actual) {
        assertMasterTenantAutoGeneratedPropertiesEquals(expected, actual);
        assertMasterTenantAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertMasterTenantAllUpdatablePropertiesEquals(MasterTenant expected, MasterTenant actual) {
        assertMasterTenantUpdatableFieldsEquals(expected, actual);
        assertMasterTenantUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertMasterTenantAutoGeneratedPropertiesEquals(MasterTenant expected, MasterTenant actual) {
        assertThat(expected)
            .as("Verify MasterTenant auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertMasterTenantUpdatableFieldsEquals(MasterTenant expected, MasterTenant actual) {
        assertThat(expected)
            .as("Verify MasterTenant relevant properties")
            .satisfies(e -> assertThat(e.getCode()).as("check code").isEqualTo(actual.getCode()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertMasterTenantUpdatableRelationshipsEquals(MasterTenant expected, MasterTenant actual) {
        // empty method
    }
}
