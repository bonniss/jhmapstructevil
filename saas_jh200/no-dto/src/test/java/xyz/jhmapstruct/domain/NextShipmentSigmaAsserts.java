package xyz.jhmapstruct.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class NextShipmentSigmaAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertNextShipmentSigmaAllPropertiesEquals(NextShipmentSigma expected, NextShipmentSigma actual) {
        assertNextShipmentSigmaAutoGeneratedPropertiesEquals(expected, actual);
        assertNextShipmentSigmaAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertNextShipmentSigmaAllUpdatablePropertiesEquals(NextShipmentSigma expected, NextShipmentSigma actual) {
        assertNextShipmentSigmaUpdatableFieldsEquals(expected, actual);
        assertNextShipmentSigmaUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertNextShipmentSigmaAutoGeneratedPropertiesEquals(NextShipmentSigma expected, NextShipmentSigma actual) {
        assertThat(expected)
            .as("Verify NextShipmentSigma auto generated properties")
            .satisfies(e -> assertThat(e.getId()).as("check id").isEqualTo(actual.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertNextShipmentSigmaUpdatableFieldsEquals(NextShipmentSigma expected, NextShipmentSigma actual) {
        assertThat(expected)
            .as("Verify NextShipmentSigma relevant properties")
            .satisfies(e -> assertThat(e.getTrackingNumber()).as("check trackingNumber").isEqualTo(actual.getTrackingNumber()))
            .satisfies(e -> assertThat(e.getShippedDate()).as("check shippedDate").isEqualTo(actual.getShippedDate()))
            .satisfies(e -> assertThat(e.getDeliveryDate()).as("check deliveryDate").isEqualTo(actual.getDeliveryDate()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertNextShipmentSigmaUpdatableRelationshipsEquals(NextShipmentSigma expected, NextShipmentSigma actual) {
        assertThat(expected)
            .as("Verify NextShipmentSigma relationships")
            .satisfies(e -> assertThat(e.getTenant()).as("check tenant").isEqualTo(actual.getTenant()));
    }
}
